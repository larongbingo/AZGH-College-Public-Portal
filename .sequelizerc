// tslint:disable: no-var-requires
// tslint:disable-next-line: no-submodule-imports
require("ts-node/register");

const path = require("path");

module.exports = {
  "config":          path.resolve("src", "common", "config.ts"),
  "migrations-path": path.resolve("build", "common", "database", "migrations"),
  "models-path":     path.resolve("build", "common", "database", "models"),
  "seeders-path":    path.resolve("build", "common", "database", "seeders"),
};
